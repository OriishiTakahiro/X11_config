X11 config files
```sh
$sudo ln -s 00-keyboard.conf /etc/X11/xorg.conf.d/00-keyboard.conf                                                                                                                                                                                                     [master|✓]
$ sudo ln -s 30-touchpad.conf /etc/X11/xorg.conf.d/30-touchpad.conf   
```
